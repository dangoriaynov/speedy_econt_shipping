/**
 * Frontend JavaScript for Speedy & Econt Shipping plugin.
 *
 * @package Speedy_Econt_Shipping
 * @since   2.0.0
 */

/* global jQuery, sesh_params */

(function($) {
	'use strict';

	/**
	 * SESH Frontend handler.
	 */
	var SESH_Frontend = {
		/**
		 * Initialize.
		 */
		init: function() {
			// Placeholder for future checkout office selection functionality.
			// The legacy JavaScript in js.php handles this for now.
		}
	};

	// Initialize on document ready.
	$(document).ready(function() {
		SESH_Frontend.init();
	});

})(jQuery);
